#include<iostream>
#include<cstring>
#include<algorithm>
#include<vector>
#include<queue>

using namespace std;

int main()
{
	cout << 120 << endl << 125 << endl << 150 << endl << 316 << endl;
	/*for (int a = 1; a <= 711; a++) {
		for (int b = a; b <= 711; b++) {
			for (int c = b; c <= 711; c++) {
				for (int d = c; d <= 711; d++) {
					if (a + b + c + d == 711 && a * b * c * d == 711000000) {
						cout << a << ' ' << b << ' ' << c << ' ' << d << endl;
						return 0;
					}
				}
			}
		}
	}*/
	return 0;
}
