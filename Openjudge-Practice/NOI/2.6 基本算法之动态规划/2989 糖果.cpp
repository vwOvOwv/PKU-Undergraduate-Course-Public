#include<iostream>
#include<cstring>
#include<algorithm>
#include<climits>

using namespace std;

int n, k;
int f[110][110];//f[i][j]????i????????????k??????j???????
int w[110];

int main()
{
	cin >> n >> k;
	int maxi = 0;
	for (int i = 1; i <= n; i++) {
		cin >> w[i];
	}
	memset(f, -1, sizeof(f));//????????j??
	f[0][0] = 0;
	for (int i = 1; i <= n; i++) {
		for (int j = 0; j < k; j++) {
			if (f[i - 1][j] >= 0) {
				f[i][j] = max(f[i][j], f[i - 1][j]);//?????i?????
				f[i][(j + w[i] % k) % k] = max(f[i][(j + w[i] % k) % k], f[i - 1][j] + w[i]);//???i?????????????????
			}
		}
	}
	cout << f[n][0];
	return 0;
}
